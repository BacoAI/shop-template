# 本機測試用的環境變數。
# 複製成 .dev.vars 再改（.dev.vars 已經排除在 git 之外，不會被傳出去）。
#
# ⚠ 後台密碼不在這裡設。第一次登入用 admin / admin，
#    系統會要你當場換成自己的帳號名與密碼。

# 選填：有人下單時寄一封通知信給你。到 resend.com 申請，不填就不寄，訂單照樣成立。
# RESEND_API_KEY=
# MAIL_FROM=
